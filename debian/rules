#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_clean:
	cargo clean || true

override_dh_auto_build:
	cargo build --release

override_dh_auto_test:
	# Skip tests during package build

override_dh_auto_install:
	install -D -m 0755 target/release/debsign debian/debsign-rs/usr/bin/debsign
